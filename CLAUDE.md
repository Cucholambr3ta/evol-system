# Evol-DD — Operation Manifest

**Trigger:** `/evol`

---

## Governing Law

`docs/constitucion.md` is the supreme law. Read it before anything else. The
Constitution is the single source of truth for governance, workflow, and engineering
standards.

---

## Memory Requirement (Constitution Art. 3 — Flight Recorder)

**Mandatory:** Read `memoria.md` at the start of every session.

Every significant work session ends by updating `memoria.md` with:
- Decisions made
- Artifacts produced
- Open issues
- Next steps

`memoria.md` is the project truth. `lecciones.md` holds cross-project lessons.

---

## Workflow Table

| Command | Produces | Phase |
|---------|----------|-------|
| `/evol briefing` | `BRIEFING.md` | Briefing |
| `/evol spec` | `SPEC.md` + `docs/requisitos/` | Spec |
| `/evol plan` | `PLAN.md` + `CASOS_GHERKIN.md` | Plan |
| `/evol build` | Code + `docs/diagramas/` | Build |
| `/evol qa` | `docs/qa/REPORTE_QA.md` | QA |
| `/evol retro` | `LECCIONES.md` update | Retro |
| `/evol research` | `RESEARCH.md` | Continuous |
| `/evol gate status` | Gate approval status | Any |
| `/evol gate approve` | HMAC-signed approval | Any |
| `/evol doc` | Full documentation set | Any |

---

## Scripts Table

| Script | Function |
|--------|---------|
| `evol-init` | Bootstrap project from template |
| `evol-start` | Start system, load last session |
| `evol-doctor` | Diagnostic: deps, structure, mode |
| `evol-gate` | HMAC-SHA256 gate keeper |
| `evol-state` | SQLite state store |
| `evol-orchestrate` | Multi-agent runtime |
| `evol-agent` | Create / invoke / retire / recall ephemeral agents |
| `evol-evolve` | Auto-generate skills, sync community |
| `evol-research` | Autonomous research proposals |
| `evol-memory` | Load / summarize / compact / search / gc / `sprint-close --sprint=NN` (crea acuerdos/memoria + lecciones por sprint) |
| `evol-gitflow` | GitFlow automatizado: `setup --mode=dev\|collab`, `sprint-start`, `sprint-close`, `pre-push`, `status` |
| `evol-discipline-check` | Valida CONTENIDO por disciplina SDD/FDD/DDD/BDD/TDD/STDD/SecDD. Activar: `EVOL_DISCIPLINE=1` |
| `evol-lessons` | Add / search / suggest-fix / apply-fix |
| `evol-eval` | Run eval harness |
| `evol-shield` | Security audit |
| `evol-flow` | Execute declarative flows |
| `evol-adapt` | Generate IDE configs (claude-code, opencode, cursor, windsurf, vscode-copilot, antigravity, codex) |
| `evol-brand` | Apply branding |
| `evol-update` | Update mechanism: `status` / `check` / `apply`. Pip-mode: upgrades package + propagates workflows + regenerates IDE configs. Legacy-mode: copies scripts from EVOL_SOURCE_DIR + propagates. |

---

## Quality Guidelines

### Absolute Portability
All file paths must be **relative**. No absolute paths. Scripts and configs work
on any machine without modification.

### Zero Duplicates
Every piece of knowledge exists in exactly one place:
- **SSoT:** `.agent/workflows/` (versioned, never generated)
- **IDE outputs:** generated by `evol-adapt.sh` from SSoT
- **Registry:** `prompts/agents/registry.json` (auto-generated from core agents)

### Gated Pipeline
Never chain long flows without checkpoints. Require explicit "APPROVED" for
structural changes or phase transitions. Use `evol-gate` for HMAC-signed approvals.

---

## Anti-Patterns

- **NEVER** reference `xdd-*` or X-DD in artifacts
- **NEVER** use absolute file paths
- **NEVER** commit secrets, keys, or runtime state

---

## Mode Detection

```bash
evol-doctor          # Human output: COMPLETO / BASE
evol-doctor --json   # Machine output: "Memoria Persistente_mode"
```

- **COMPLETO:** Memoria Persistente CLI available (semantic search, pattern extraction)
- **BASE:** No Memoria Persistente — full pipeline still works, learn manually
